# AMP_predictor

User inputs a aa FASTA sequence and the model predicts whether it is an antimicrobial peptide or not

# For the marker

-   Please navigate to /notebooks to see how I selected, trained and validated my model
-   The trained model is saved in /model
-   By using the R script found in R/ the user will upload an amino acid .FASTA file (pre-loaded already as mock.fasta). Then it should open an R shiny plotting features and AMP predictions that will also be saved into output/
